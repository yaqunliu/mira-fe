# 环境变量示例文件
# 复制此文件为 .env.local 或 .env.production 并根据实际情况修改配置

# 运行环境
NODE_ENV=production

# API 配置
# 后端 API 的基础 URL，不包含路径，如：https://api.example.com
NEXT_PUBLIC_API_URL=https://your-api-domain.com

# 应用端口（可选，默认 3000）
PORT=3000

# Supabase 配置
# 本地开发：运行 `supabase status` 查看配置
# 生产环境：使用 Supabase 项目设置中的配置
NEXT_PUBLIC_SUPABASE_URL=http://127.0.0.1:54321
NEXT_PUBLIC_SUPABASE_ANON_KEY=你的anon_key

# 数据库配置（如果需要）
# DATABASE_URL=postgresql://user:password@localhost:5432/novel2video

# 认证配置（如果需要）
# NEXTAUTH_URL=https://your-domain.com
# NEXTAUTH_SECRET=your-secret-key

# 文件上传配置
# MAX_FILE_SIZE=104857600  # 100MB in bytes
# UPLOAD_DIR=./uploads

# 其他配置...
# 请根据实际需求添加其他环境变量

